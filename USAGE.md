# Word to HWP 변환 시스템 - 사용 가이드

## 시스템 개요

이 시스템은 Microsoft Word 파일(.docx, .doc)을 한컴오피스 한글 파일(.hwp)로 변환하는 웹 기반 애플리케이션입니다.

## 주요 기능

1. **워드 파일 업로드 및 HWP 변환**
   - DOCX/DOC 파일을 업로드하여 HWP 파일로 변환
   - 드래그 앤 드롭 지원
   - 변환 후 즉시 다운로드 가능

2. **템플릿 관리**
   - 자주 사용하는 양식을 템플릿으로 저장
   - 저장된 템플릿으로 새 HWP 파일 생성
   - 템플릿 목록 조회 및 삭제

3. **다크 테마 UI**
   - 눈이 편한 어두운 색감의 사용자 인터페이스
   - 직관적인 탭 기반 네비게이션

## 시스템 요구사항

### 필수 사항
- **운영체제**: Windows 10 이상
- **한컴오피스**: 한글 2014 이상 (현재 버전: 9.0.0.562 확인됨)
- **Python**: 3.8 이상
- **브라우저**: Chrome, Firefox, Edge 등 최신 브라우저

### 선택 사항
- Node.js 16 이상 (프론트엔드 개발용)

## 설치 방법

### 1. Python 의존성 설치

```bash
cd backend
pip install -r requirements.txt
```

필요한 패키지:
- fastapi==0.109.0
- uvicorn==0.27.0
- python-multipart==0.0.6
- pywin32==306 (한컴오피스 COM 자동화)
- python-docx==1.1.0
- aiofiles==23.2.1
- pydantic==2.5.3
- python-dotenv==1.0.0

### 2. 디렉토리 구조 확인

프로젝트 구조:
```
Claude_Code_Vibe_Coding/
├── backend/
│   ├── main.py              # FastAPI 메인 애플리케이션
│   ├── hwp_converter.py     # 한컴오피스 COM 자동화 모듈
│   └── requirements.txt     # Python 의존성
├── frontend/
│   ├── index.html          # 메인 HTML
│   ├── style.css           # 다크 테마 CSS
│   └── app.js              # JavaScript
├── templates/              # 저장된 템플릿
├── uploads/                # 업로드된 파일
├── outputs/                # 변환된 HWP 파일
└── README.md
```

## 실행 방법

### 서버 시작

```bash
cd backend
python main.py
```

서버가 시작되면 다음과 같이 표시됩니다:
```
============================================================
Word to HWP 변환 시스템 서버 시작
============================================================
주소: http://localhost:8000
API 문서: http://localhost:8000/docs
============================================================
```

### 웹 애플리케이션 접속

브라우저에서 다음 주소로 접속:
- **메인 페이지**: http://localhost:8000
- **API 문서**: http://localhost:8000/docs (FastAPI Swagger UI)

## 사용 방법

### 1. 워드 파일을 HWP로 변환

1. 메인 페이지에서 "파일 변환" 탭 선택
2. 업로드 영역을 클릭하거나 파일을 드래그 앤 드롭
3. DOCX 또는 DOC 파일 선택
4. "HWP로 변환" 버튼 클릭
5. 변환 완료 후 "다운로드" 버튼으로 HWP 파일 다운로드

### 2. 템플릿으로 저장

1. 워드 파일 업로드
2. "템플릿으로 저장" 버튼 클릭
3. 템플릿 이름 입력 (예: "회사 공문서 양식")
4. "저장" 버튼 클릭

### 3. 템플릿 사용

1. "템플릿 관리" 탭으로 이동
2. 저장된 템플릿 목록에서 원하는 템플릿 선택
3. "사용" 버튼 클릭
4. 자동으로 HWP 파일 생성
5. "다운로드" 버튼으로 파일 다운로드

### 4. 템플릿 삭제

1. "템플릿 관리" 탭에서 삭제할 템플릿 선택
2. "삭제" 버튼 클릭
3. 확인 메시지에서 "확인" 클릭

## API 엔드포인트

### 1. 시스템 상태 확인
```
GET /api/health
```
응답:
```json
{
  "status": "ok",
  "hwp_installed": true,
  "version": "9, 0, 0, 562"
}
```

### 2. 파일 업로드
```
POST /api/upload
Content-Type: multipart/form-data

file: [DOCX/DOC 파일]
```

### 3. HWP 변환
```
POST /api/convert
Content-Type: multipart/form-data

file_id: [업로드된 파일 ID]
```

### 4. HWP 다운로드
```
GET /api/download/{file_id}
```

### 5. 템플릿 저장
```
POST /api/template/save
Content-Type: multipart/form-data

file: [파일]
template_name: [템플릿 이름]
```

### 6. 템플릿 목록 조회
```
GET /api/templates
```

### 7. 템플릿 사용
```
POST /api/template/use/{template_id}
```

### 8. 템플릿 삭제
```
DELETE /api/template/{template_id}
```

## 한컴오피스 버전 호환성

이 시스템은 다음 한컴오피스 한글 버전과 호환됩니다:
- 한글 2014 (버전 9.x)
- 한글 2018 (버전 10.x)
- 한글 2020 (버전 11.x)
- 한글 2022 (버전 12.x)

**현재 시스템에서 확인된 버전**: 한글 2014 (9.0.0.562)

## 기술 스택

### 백엔드
- **Python 3.8+**
- **FastAPI**: 고성능 웹 프레임워크
- **pywin32**: Windows COM 자동화
- **Uvicorn**: ASGI 서버

### 프론트엔드
- **HTML5**
- **CSS3**: 다크 테마 커스텀 디자인
- **Vanilla JavaScript**: 프레임워크 없이 순수 JavaScript

### 변환 엔진
- **한컴오피스 COM 자동화**: Windows COM을 통한 한글 프로그램 제어
- 한글의 `Open()` 및 `SaveAs()` 메서드를 사용한 파일 변환

## 문제 해결

### 1. 한컴오피스 한글이 설치되어 있지 않다고 표시될 때

**증상**: 시스템 상태가 "한컴오피스 한글 미설치"로 표시

**해결 방법**:
- 한컴오피스 한글 2014 이상이 설치되어 있는지 확인
- 한글 프로그램이 올바르게 등록되어 있는지 확인
- 관리자 권한으로 Python 스크립트 실행 시도

### 2. 변환이 실패할 때

**가능한 원인**:
- 워드 파일이 손상되었거나 비정상적인 형식
- 한글 프로그램이 이미 실행 중
- 파일 경로에 특수문자가 포함됨

**해결 방법**:
- 워드 파일을 다시 저장한 후 시도
- 실행 중인 한글 프로그램 종료
- 파일명에서 특수문자 제거

### 3. 인코딩 오류

Windows 환경에서 한글이 깨지는 경우, 코드에 UTF-8 인코딩 처리가 포함되어 있습니다.

### 4. 포트가 이미 사용 중일 때

다른 프로그램이 포트 8000을 사용하는 경우:
```python
# main.py 마지막 줄 수정
uvicorn.run(app, host="0.0.0.0", port=8001, log_level="info")
```

## 보안 고려사항

1. **파일 업로드 제한**: DOCX, DOC 파일만 허용
2. **파일 크기 제한**: FastAPI 기본 설정 사용
3. **파일 검증**: 확장자 및 MIME 타입 확인
4. **CORS 설정**: 현재는 모든 출처 허용 (프로덕션에서는 제한 필요)

## 성능 최적화

1. **비동기 처리**: FastAPI의 async/await 사용
2. **파일 스트리밍**: 큰 파일 업로드 시 청크 단위 처리
3. **캐싱**: 정적 파일 캐싱 활용

## 향후 개발 계획

- [ ] 일괄 변환 기능
- [ ] 변환 진행률 표시
- [ ] 변환 히스토리 관리
- [ ] 클라우드 스토리지 연동
- [ ] 더 나은 오류 처리 및 로깅
- [ ] 토스트 알림 시스템
- [ ] 다국어 지원

## 라이선스

이 프로젝트는 내부 사용을 위해 개발되었습니다.

## 지원

문제가 발생하거나 개선 사항이 있으면 개발자에게 문의하세요.

---

**개발 완료일**: 2025년 10월 16일
**개발자**: Claude Code Assistant
**버전**: 1.0.0
